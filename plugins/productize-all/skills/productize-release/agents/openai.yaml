interface:
  display_name: "Release"
  short_description: "Release gate for ship readiness, deployment decision, changelog needs, rollback risk, versioning, launch..."
  brand_color: "#4F46E5"
  default_prompt: "Use $productize-release with my product context."

policy:
  allow_implicit_invocation: false
