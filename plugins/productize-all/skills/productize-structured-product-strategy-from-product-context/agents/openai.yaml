interface:
  display_name: "Structured product strategy from product context"
  short_description: "Structured product strategy from product context. Use when the user needs a product workflow for product strategy..."
  brand_color: "#059669"
  default_prompt: "Use $productize-structured-product-strategy-from-product-context with my product context."

policy:
  allow_implicit_invocation: false
