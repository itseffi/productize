interface:
  display_name: "0-1 Product Build Loop"
  short_description: "Productize 0-1 orchestrator for turning a raw product idea or new capability bet into a working first product slice...."
  brand_color: "#0F766E"
  default_prompt: "Use $0-1 with my product context."

policy:
  allow_implicit_invocation: true
