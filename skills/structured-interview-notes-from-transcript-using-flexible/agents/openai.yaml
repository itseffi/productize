interface:
  display_name: "Structured interview notes from transcript using flexible frameworks"
  short_description: "Structured interview notes from transcript using flexible frameworks. Use when the user needs a product workflow for..."
  brand_color: "#0D9488"
  default_prompt: "Use $structured-interview-notes-from-transcript-using-flexible with my product context."

policy:
  allow_implicit_invocation: true
