interface:
  display_name: "Product strategy review from draft documents"
  short_description: "Product strategy review from draft documents. Use when the user needs a product workflow for product strategy..."
  brand_color: "#0891B2"
  default_prompt: "Use $product-strategy-review-from-draft-documents with my product context."

policy:
  allow_implicit_invocation: true
