interface:
  display_name: "Map power dynamics before meetings"
  short_description: "Map power dynamics before meetings. Use when the user needs a product workflow for stakeholder management related to..."
  brand_color: "#0891B2"
  default_prompt: "Use $map-power-dynamics-before-meetings with my product context."

policy:
  allow_implicit_invocation: true
