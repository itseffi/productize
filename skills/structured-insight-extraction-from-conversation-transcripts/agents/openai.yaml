interface:
  display_name: "Structured insight extraction from conversation transcripts"
  short_description: "Structured insight extraction from conversation transcripts. Use when the user needs a product workflow for..."
  brand_color: "#0891B2"
  default_prompt: "Use $structured-insight-extraction-from-conversation-transcripts with my product context."

policy:
  allow_implicit_invocation: true
