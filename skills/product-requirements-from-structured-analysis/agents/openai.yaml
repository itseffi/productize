interface:
  display_name: "Product requirements from structured analysis"
  short_description: "Product requirements from structured analysis. Use when the user needs a product workflow for stakeholder management..."
  brand_color: "#4F46E5"
  default_prompt: "Use $product-requirements-from-structured-analysis with my product context."

policy:
  allow_implicit_invocation: true
