interface:
  display_name: "Post-launch feedback into v2 improvements synthesis"
  short_description: "Post-launch feedback into v2 improvements synthesis. Use when the user needs a product workflow for business..."
  brand_color: "#0D9488"
  default_prompt: "Use $post-launch-feedback-into-v2-improvements-synthesis with my product context."

policy:
  allow_implicit_invocation: true
