interface:
  display_name: "Structured requirements from conversation transcripts"
  short_description: "Structured requirements from conversation transcripts. Use when the user needs a product workflow for business..."
  brand_color: "#4F46E5"
  default_prompt: "Use $structured-requirements-from-conversation-transcripts with my product context."

policy:
  allow_implicit_invocation: true
