interface:
  display_name: "Bug prioritization against work in progress"
  short_description: "Bug prioritization against work in progress. Use when the user needs a product workflow for decision making related..."
  brand_color: "#4F46E5"
  default_prompt: "Use $bug-prioritization-against-work-in-progress with my product context."

policy:
  allow_implicit_invocation: true
