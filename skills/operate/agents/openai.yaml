interface:
  display_name: "Productize Operate"
  short_description: "Productize operate playbook for the continuous production loop after a deploy. Use when a shipped product needs..."
  brand_color: "#7C3AED"
  default_prompt: "Use $operate with my product context."

policy:
  allow_implicit_invocation: true
