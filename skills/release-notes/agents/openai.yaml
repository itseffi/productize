interface:
  display_name: "Release Notes"
  short_description: "Release Notes. Use when turning tickets, PRDs, changelogs, or shipped work into clear user-facing release notes or..."
  brand_color: "#4F46E5"
  default_prompt: "Use $release-notes with my product context."

policy:
  allow_implicit_invocation: true
